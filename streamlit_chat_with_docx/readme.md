# Application for LLM integration to chat with text file
## step 1: Installations
```bash
python -m venv .venv
```
for activation of envieronment
```bash
source .venv/Scripts/activate
```
## step 2: libraries
```bash
pip install -r requirements.txt
```

### Prompt 
act as developer of python for Gen AI applications development using streamlit.
Make sure develope up to date applications.
use .venv as virtual environment and let me know if i need to install any library only edit this app.py and create an application where user can upload a txt document and then use open AI key to chat that document.
Make a nicer interface for application and improve the querry for final answer in terms of speed. 
use streamlit cashe for better management of data on ram.
save the context in memory whenever user wants to ga back provide that option
now proceed with editing that application 